- Replaced the Oneiron factory's seat and test clocks with an idle detector on the seat stream (`launcher.idleMs`), removed the writer round cap, added `launcher.buildHosts` and a cargo wrapper that runs a worktree's builds on the first reachable build host, made factory seats daemon-hosted so they can be opened while they run, made `factory launch` rewrite known tickets and re-import queued actions, and let `factory resolve` take several attempt ids.
