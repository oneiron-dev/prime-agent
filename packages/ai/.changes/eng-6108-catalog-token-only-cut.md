- Cut the shipped MCP service catalog to one-click DCR connectors or a simple paste-an-API-key/token service (2026-09-16 product decision, from live picker testing: "things like cockroachdb cloud still need mcp/ 'requires provider credentials supplied as headers'? i told you to remove all that stuff?"): the 5 remaining survivors that were neither — the 3 tenant configs (CockroachDB Cloud's per-cluster `mcp-cluster-id` header, Dynatrace's `${DT_ENVIRONMENT}` instance URL, Sourcegraph's `${SOURCEGRAPH_ENDPOINT}` instance URL), the 1 legacy-SSE endpoint the generic runtime cannot connect to at all (PayPal Sandbox) and the 1 api-key entry that shipped zero setup fields, so the user had nothing to paste (Render, whose upstream config carries only a Claude-specific OAuth client id Prime must not reuse) — are excluded via the documented `excludedServers` list in `mcp-catalog/overrides.json`, each with a per-entry reason citing the decision and the entry's own evidence; all shipped entries are plain streamable-http endpoints with zero templates, zero SSE and zero stdio adapters (ENG-6108; the follow-up single-credential cut below narrows the shipped counts further).
- The importer enforces the token-only cut structurally: a user-setup entry ships only when its requirement is literally `bearer-token` or `api-key` AND it collects at least one bearer-token/api-key field, so a tenant config, an unsupported transport or a key/token entry with nothing to paste can never silently re-derive; an entry the readiness pass never classified now fails closed too, instead of slipping past every shipping guard (ENG-6108).
- Rewrote all 13 surviving setup reasons as picker copy for a human: one line, starting with "paste", naming the entry's real setup field. "requires provider credentials supplied as headers" becomes an honest "paste …" line naming what to paste; "requires an auth token supplied via environment variable" becomes "paste your Zoom Chat access token (ZOOM_CHAT_MCP_ACCESS_TOKEN)" and friends; GitHub no longer stutters the same clause twice ("paste a GitHub personal access token (GITHUB_PAT_TOKEN or GITHUB_PERSONAL_ACCESS_TOKEN)"); Zoom Meetings drops the upstream placeholder note that never changed what the user does — the placeholder client id stays in the pinned fixture as evidence. A test pins the shape: single line, <= 100 chars, no repeated clause, no "upstream", and every reason names a field the entry actually collects (ENG-6108).
