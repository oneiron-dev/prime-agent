- Cut the shipped MCP service catalog to zero-app self-serve only (2026-09-14 product decision: Prime maintains no provider OAuth apps): the 16 providers that require a provider-registered OAuth client (Figma, Slack, Gmail, Google Calendar, Google Drive, MongoDB Atlas) or whose self-serve path stayed honestly unknown (Shopify, HubSpot, LogRocket, Mapbox Docs, Adobe for Creativity, Spotify Confidence, SumUp, Synthflow) are excluded via the documented `excludedServers` list in `mcp-catalog/overrides.json` with one entry per upstream record and a per-entry reason citing the decision; the catalog now ships 124 entries — 71 dynamic-client-registration (oauth-ready) and 53 user-token/key (user-setup) connectors (ENG-6108).
- The importer now enforces the zero-app cut structurally: an entry that still classifies prime-restricted or unknown fails the import with a curation prompt (exclude with a documented reason, or curate readiness with evidence), so a provider-client gate or an unverified self-serve path can never ship silently; the gated-DCR honesty machinery (advertisedRegistrationGated, explicit-false metadata semantics) is kept and now unit-tested directly, the pinned source snapshots and audit evidence stay committed as history, and the audit script retires — never drops — live registration-attempt evidence for servers that leave the audit target set (ENG-6108).
