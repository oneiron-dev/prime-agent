- Cut the shipped MCP service catalog to one-click DCR or user token/key only (2026-09-15 product decision): the 49 entries that are neither — the 14 registered-client entries whose authorization servers accept only secret-bearing client auth (Airwallex and its sandbox, Atlan, GitLab, Hugging Face, LegalZoom, Lusha, Miro, monday.com, PlanetScale, Supabase, Vercel, Windsor.ai, ZoomInfo), the 32 local-runtime stdio adapters (AWS/Azure/GCP toolchain proxies, Firebase, Firestore, BigQuery, Cloud SQL, Spanner, Dataproc, Pinecone, Discord, Telegram, Semgrep, Terraform, Zscaler, …) and the 3 url-only tenant templates (ActiveCampaign, JFrog, Pigment) — are excluded via the documented `excludedServers` list in `mcp-catalog/overrides.json`, one entry per upstream record (64 keys, both upstream sides of the merged providers), each with a per-entry reason citing the decision and the specific evidence; the catalog now ships 75 entries — 57 one-click dynamic-client-registration (oauth-ready) and 18 user token/key (user-setup) connectors, with zero prime-restricted/unknown and zero stdio adapters (ENG-6108).
- The importer enforces the final cut structurally: a user-setup entry whose requirement is not token/key-shaped — bearer-token/api-key credentials, a tenant config carrying a genuine non-url field, or a legacy-transport entry that still collects a token/key — fails the import with a curation prompt, so a registered-client demotion, a local-runtime adapter or a url-only tenant template can never silently re-derive into the catalog; the auth-methods honesty machinery (decideClientAuthMethod parity, confidential-only demotion, tokenAuthMethods capture) stays active for the survivors, and the pinned source snapshots and audit evidence stay committed as history (ENG-6108).
