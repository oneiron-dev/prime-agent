- Single-credential cut (2026-09-16, ENG-6108 Bugbot findings): the generic MCP runtime sends exactly one `Authorization: Bearer` per connection, so the shipped paste-a-key class now collects exactly ONE credential. GitHub's two setup fields (GITHUB_PAT_TOKEN, GITHUB_PERSONAL_ACCESS_TOKEN) are curated as ALTERNATIVE NAMES for one personal access token via a shared `credentialSet` ("github-pat") in `mcp-catalog/overrides.json` — the paste flow prompts once, stores the value under the first alternative's id — and the genuinely distinct named-header pairs that one bearer can never authenticate (Datadog's DD_API_KEY + DD_APPLICATION_KEY, Cloudinary MediaFlows' cld-api-key + cld-secret) are excluded via the documented `excludedServers` list, each with a reason citing the named-header evidence. The catalog now ships 68 entries: 57 one-click dynamic-client-registration (oauth-ready) and 11 paste-a-token (user-setup) connectors (ENG-6108).
- The importer enforces the single-credential cut structurally: a shipped requires-setup entry that collects more than one distinct required credential fails the import with an actionable curation message (mark alternative names with a shared `setupFields` `credentialSet`, or exclude the entry with a documented reason), so an unmarked multi-credential entry can never silently re-derive; a test pins the guard by lifting the Datadog and Cloudinary exclusions and asserting the import throws (ENG-6108).
- Every surviving setup reason is honest single-credential copy: one line, "paste a …" naming the one credential the user actually supplies; the Datadog/Cloudinary multi-key reasons are gone with their entries, and GitHub's reason names the two alternative names for the same token instead of listing two requirements (ENG-6108).
